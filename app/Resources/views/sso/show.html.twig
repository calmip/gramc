{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sso</h1>

    <table>
        <tbody>
            <tr>
                <th>Eppn</th>
                <td>{{ sso.eppn }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('sso_edit', { 'id': sso.eppn }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
